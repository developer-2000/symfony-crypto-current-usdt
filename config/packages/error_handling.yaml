# ---------------------------------------------------------------------------
# 9. Обработка ошибок и границы (политика, реализация в коде)
# ---------------------------------------------------------------------------
# Документация правил. Параметры лимитов — в portfolio_history_api.yaml.
#
# 9.1 Binance (команда app:portfolio:snapshot):
#   — Таймаут / 5xx / неверный JSON: снапшот не сохраняется, ошибка логируется
#     (канал portfolio), выбрасывается App\Exception\BinanceApiException.
#   — Команда ловит исключение, пишет в лог, возвращает Command::FAILURE (cron
#     видит ненулевой код выхода и может отправить уведомление).
#   — Реализация: BinancePriceService, PortfolioSnapshotCommand.
#
# 9.2 API GET /api/portfolio/history:
#   — Нет данных в диапазоне: ответ 200 и тело [] (не 404).
#   — Невалидные параметры (hours вне 1..max, from > to, неверный формат дат,
#     смешанные hours и from/to): ответ 400 с полем "error" и текстом.
#   — Реализация: PortfolioHistoryController.
# ---------------------------------------------------------------------------

parameters: {}
