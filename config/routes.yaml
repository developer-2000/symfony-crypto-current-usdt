# Web — like Laravel routes/web.php
web:
    resource: ../src/Controller/Web/
    type: attribute

# API with prefix /api — like Laravel routes/api.php
api:
    resource: ../src/Controller/Api/
    type: attribute
    prefix: /api
