# Variables for APP_ENV=test (override .env).
# To run tests in Docker create .env.test.local with DATABASE_URL pointing to db host.
# Locally: MySQL on 127.0.0.1 or override in .env.test.local.
DATABASE_URL="mysql://root:@127.0.0.1:3306/app_test?serverVersion=8.0&charset=utf8mb4"
DEFAULT_URI="http://localhost"
