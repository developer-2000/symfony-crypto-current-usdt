# Переменные для APP_ENV=test (переопределяют .env).
# Для запуска тестов в Docker создайте .env.test.local с DATABASE_URL на хост db.
# Локально: MySQL на 127.0.0.1 или переопределите в .env.test.local.
DATABASE_URL="mysql://root:@127.0.0.1:3306/app_test?serverVersion=8.0&charset=utf8mb4"
DEFAULT_URI="http://localhost"
