###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
APP_SHARE_DIR=var/share
###> symfony/routing ###
DEFAULT_URI=http://localhost:8080
###> database ###
DATABASE_URL="mysql://root:@db:3306/lytvynov_crypto?serverVersion=8.0&charset=utf8mb4"
###> redis ###
REDIS_URL=redis://redis:6379

###> portfolio & binance (все переменные проекта — только в .env) ###
PORTFOLIO_BTC=1
PORTFOLIO_ETH=10
PORTFOLIO_SOL=50
PORTFOLIO_USDT=5000
BINANCE_API_BASE_URL=https://api.binance.com
BINANCE_SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT

# Гранулярность снапшота: hour | minute (новая запись каждую минуту) | second (тест).
PORTFOLIO_SNAPSHOT_GRANULARITY=minute

# Cron в контейнере: снапшот каждые N минут. 1 = раз в минуту, 60 = раз в час.
CRON_SNAPSHOT_MINUTES=1

###> API GET /api/portfolio/history (лимиты и значения по умолчанию) ###
# Максимум часов для параметра ?hours= (защита от тяжёлых выборок)
PORTFOLIO_HISTORY_MAX_HOURS=720
# Значение по умолчанию для hours на главной (для быстрой проверки можно 1–2)
PORTFOLIO_HISTORY_DEFAULT_HOURS=2

###> API GET /api/events ###
# EVENTS_API_LIMIT_DEFAULT=50
# EVENTS_API_LIMIT_MAX=100

###> symfony/mercure-bundle ###
# URL хаба для публикации с бэка (из контейнера app — mercure:80)
MERCURE_URL=http://mercure:80/.well-known/mercure
# Публичный URL хаба для подписки из браузера (порт 3000 проброшен с контейнера mercure)
MERCURE_PUBLIC_URL=http://localhost:3000/.well-known/mercure
# Секрет для подписи JWT (тот же ключ задать в контейнере mercure: MERCURE_PUBLISHER_JWT_KEY, MERCURE_SUBSCRIBER_JWT_KEY)
MERCURE_JWT_SECRET=!ChangeMe!
###< symfony/mercure-bundle ###
